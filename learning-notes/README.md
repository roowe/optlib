# Learning Notes

This directory stores background notes for learning the mathematics behind
Optlib and the mathlib files it depends on.

The intended workflow is:

- Use Typst for human-readable Chinese mathematical notes.
- Use Lean files, when needed, for checked examples and exercises.
- Use `index.yaml` as the machine-readable map for generated notes.

Current Typst notes live under `typst/notes`.

